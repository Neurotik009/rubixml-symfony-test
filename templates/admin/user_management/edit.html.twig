{% extends 'base.html.twig' %}

{% block title %}Benutzerrollen bearbeiten{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Benutzerrollen bearbeiten</h1>
        <h3>Benutzer: {{ user.email }}</h3>

        {{ form_start(form) }}
        <div class="mb-3">
            {{ form_row(form.roles) }}
        </div>
        <button type="submit" class="btn btn-primary">Speichern</button>
        <a href="{{ path('admin_users_index') }}" class="btn btn-secondary">Zurück</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
